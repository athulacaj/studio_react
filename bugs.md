when a new project is added need to refresh the page to see it

after creating a project with full folder selected on gdrive and when trying to add a sharing link
no synced folders is showing need to edit the project and need to select the folders agian to work.